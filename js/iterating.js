(function(namesArray){
    "use strict";

    /**
     * TODO:
     * Create an array of 4 people's names and store it in a variable called
     * 'names'.
     */
//    let names = ["Jasmine", "Belle", "Cinderella", "Aurora"];
//    for (let str of names) {
//           console.log(str);
//    }
    /**
     * TODO:
     * Create a log statement that will log the number of elements in the names
     * array.
     */


//    console.log('There are ' + names.length + ' Princesses in the castle'();

    /**
     * TODO:
     * Create log statements that will print each of the names individually by
     * accessing each element's index.
     */

//    namesArray = ["Jasmine", "Belle", "Cinderella", "Aurora"];
//    console.log(namesArray[0]);
//    console.log(namesArray[1]);
//    console.log(namesArray[2]);
//    console.log(namesArray[3]);
    /**
     * TODO:
     * Write some code that uses a for loop to log every item in the names
     * array.
     */
//    var names = ["Jasmine", "Belle", "Cinderella", "Aurora"];
//    console.log(names.length); {
//    }

    /**
     * TODO:
     * Refactor your above code to use a `forEach` loop
     */
//    let names = ["Jasmine", "Belle", "Cinderella", "Aurora"];
//        namesArray.forEach(function(names) {
//            console.log("One of your instructors is named: " + names); {
//            }
    /**
     * TODO:
     * Create the following three functions, each will accept an array and
     * return an an element from it
     * - first: returns the first item in the array
     * - second: returns the second item in the array
     * - last: returns the last item in the array
     *
     * Example:
     *  > first([1, 2, 3, 4, 5]) // returns 1
     *  > second([1, 2, 3, 4, 5]) // returns 2
     *  > last([1, 2, 3, 4, 5]) // return 5
     */

//    namesArray = ['Jasmine', 'Belle', 'Cinderella', 'Aurora'];

//    console.log('There are ' + namesArray.length + ' names in the castle'));

//    console.log('The first name is: ' + namesArray[0]);

//    console.log('The second name is: ' + namesArray[1]);

//    console.log('The third name is: ' + namesArray[2]);

//    console.log('The fourth name is: ' + namesArray[3]);

//    console.log('The fifth name is: ' + namesArray[4]);

//            function first(arr) {
//            return arr[0];

//            function second(arr)
//            return arr[1]
//            function last(arr)
//            return arr[2]
//            console.log(names)[0];
//            console.log(names)[1];
//            console.log(names)[2];
//            console.log(names)[3];


})();